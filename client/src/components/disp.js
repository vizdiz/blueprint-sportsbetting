import React, {useState} from 'react';

const Team = (props) => {
    let logo = require('props.logo_path')

    return (
        <div className = "Team">
            <h5>props.name</h5>
            <img src={Image.default} alt = "image not found :("/>



        </div>


    )
}

export default Team